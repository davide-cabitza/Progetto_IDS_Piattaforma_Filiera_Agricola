package unicam.piattaforma_filiera_agricola.handler;

import java.time.LocalDateTime;
import java.util.List;
import java.util.UUID;

/**
 * Handler per la gestione del processo d'ordine.
 */
public class HandlerOrdine {
    /*
    public Ordine creaOrdine(List<RigaCarrello> righe) {
        return new Ordine(UUID.randomUUID().toString(), righe, LocalDateTime.now());
    }
    public void processaPagamento(Ordine o) { o.setStato("PAGATO"); }
    public void confermaOrdine(Ordine o) { o.setStato("CONFERMATO"); }
    public void annullaOrdiniPendenti(String idAcq) {  logica }
    public void accreditaVenditore(String idVenditore, double somma){}
*/
}


